[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "wildfire-simulator"
version = "0.1.0"
description = "Wildfire simulation"
requires-python = ">=3.14"

